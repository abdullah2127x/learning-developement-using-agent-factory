# OpenAI Agents SDK — Environment Configuration
# Copy this file to .env and fill in your API keys

# ─── OpenAI API ──────────────────────────────────────────────────────────
# Get key from: https://platform.openai.com/api-keys
# Cost: ~$0.15 per 1M input tokens, $0.60 per 1M output tokens
# Required for: agent_openai.py, agent_l3_handoff.py, agent_l4_production.py
OPENAI_API_KEY=sk-proj-...

# ─── Google Gemini API ───────────────────────────────────────────────────
# Get key from: https://aistudio.google.com/apikey
# Cost: Cheapest option (~$0.05 per 1M tokens)
# Required for: agent_gemini.py
GEMINI_API_KEY=

# ─── OpenRouter API ──────────────────────────────────────────────────────
# Get key from: https://openrouter.ai/
# Use for: Multiple models (OpenAI, Anthropic, Gemini, Meta, etc.)
# Cost: Variable by model, free options available
# Required for: agent_openrouter.py
OPENROUTER_API_KEY=

# ─── Optional: LiteLLM Configuration ──────────────────────────────────────
# Set log level for debugging (debug, info, warning, error)
LITELLM_LOG=info

# ─── Optional: Advanced Configuration ─────────────────────────────────────
# For MCP server integration:
MCP_SERVER_URL=http://localhost:8000/mcp
MCP_TOKEN=

# Database (if implementing sessions):
# DATABASE_URL=postgresql://user:password@localhost/dbname

# Environment stage:
# ENVIRONMENT=development
